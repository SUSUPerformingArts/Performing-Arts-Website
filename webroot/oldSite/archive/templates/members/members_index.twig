{# Call the header function to create the standard PA header #}
{{ header("PA Archive :: Members") }}


    <div class="well well-pa">
        <h1>Performing Arts Members</h1>
    </div>

    <div class="well well-pa">
        {% 
            include "includes/members_search.twig" with
            { "ms" :  {
                "autosearch" : true,
                "wrapform" : true,
                "samePage" : true
            }} 
        %}




    	{#% for member in members %}
    		<div>
    			<p><a href="{{ urlFor("member", { "member" : member.id }) }}">{{ member.fullName }}</a> ({{ member.societyNumber }} societies)</p>
    		</div>
    	{% endfor %#}
    </div>

{# Standard PA footer #}
{{ footer() }}
